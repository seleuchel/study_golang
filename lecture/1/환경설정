* go doc은 따로 설치해야함

go-plus, script, command line(terminal)
ㄴ atom에서 go를 실행할 수 있게하는 패키지
ㄴ script 모듈에서 어떤 언어도 다 실행할 수 있음 (단축키 shift contorl b )


Test code 
go build, run, install

## 저번 시간에 환경변수 설정 완료함 ( go env - gobin, gopath)


* go run : 코드 실행
* go build : 현재 폴더에 (.go) exe 만들기 
* go install : 배포용. 상위프로젝트명으로 exe 빌드


atom에서 레퍼런스가 있다. 
- go 함수를 알려주는 기능 vscode도 찾기 
